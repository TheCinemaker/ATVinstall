[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  # Disable secrets scanning for Firebase API keys (they are safe to be public)
  SECRETS_SCAN_SMART_DETECTION_ENABLED = "false"
