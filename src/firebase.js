// MOCK FIREBASE - OFFLINE MODE
// We export dummy objects so imports in other files don't break,
// but we won't initialize the actual Firebase app.

export const auth = {};
export const db = {};
export const storage = {};
export default {};
